"""E3.1 — Rewritten onto the GateLog/EvalContext architecture.

evaluate(ticker, df, ctx, verbose=False) -> BreakoutResult, gate-for-gate
parity with breakout_filter._evaluate (full evaluation, no short-circuit).
See CLAUDE.md EPIC E3.1.
"""
