"""E2.3 — evaluate() ported from pullback_filter._evaluate.

Pure function: evaluate(ticker, df, ctx, verbose=False) -> PullbackResult.
No I/O, no wall clock — all context comes from EvalContext. See CLAUDE.md
EPIC E2.2/E2.3.
"""
