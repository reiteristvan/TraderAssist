"""E2 — GateLog, EvalContext, QualityInfo, shared indicators.

Shared evaluation primitives used by both strategies (pullback, breakout):
the GateLog gate-accumulator (E2.1), EvalContext/QualityInfo dataclasses and
context factory (E2.2/E2.4), and helper indicators ported from
pullback_filter (E2.3). See CLAUDE.md EPIC E2.
"""
