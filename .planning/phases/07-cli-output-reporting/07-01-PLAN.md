---
phase: 07-cli-output-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scanner/seasonality.py
  - tests/test_seasonality.py
autonomous: true
requirements: [SEAS-10, SEAS-11, SEAS-12, SEAS-13]
must_haves:
  truths:
    - "pad_weeks_table returns exactly 52 rows (weeks 1..52) with the 9 SEAS-10 display columns, N/A for missing weeks and N/A ci cells for insufficient_years weeks (D-01, D-02)"
    - "render_weeks_table produces a plain-text table via to_string(index=False) with bps columns at 2 decimals (D-03, D-04)"
    - "build_summary text includes baseline mean, the significant-weeks list (or the explicit none-message), top-5/bottom-5 by delta, the multiple-comparison caveat, and an insufficient_years callout (D-05..D-09)"
    - "write_weeks_csv writes the same padded 52-row table to the given path, auto-creating parent dirs and overwriting silently (D-11, D-12, D-13)"
  artifacts:
    - "scanner/seasonality.py — pad_weeks_table, render_weeks_table, build_summary, write_weeks_csv functions"
    - "scanner/seasonality.py — module constants for the survivorship warning and multiple-comparison caveat"
    - "tests/test_seasonality.py — unit tests for all four Phase 7 functions"
  key_links:
    - "pad_weeks_table output is the single shared DataFrame consumed by both render_weeks_table and write_weeks_csv (D-12 — stdout and CSV never disagree)"
---

<objective>
Add the Phase 7 presentation functions to `scanner/seasonality.py`: pad a
`SeasonalityResult.weeks` DataFrame to a fixed 52-row display table, render it as
plain text, assemble the interpretive summary text, and write the same padded
table to CSV. All logic lives in the engine module as small composable functions
(Phase 5/6 precedent); `seasonality_by_week.py::main` (Plan 02) wires them in.

Purpose: Turn Phase 6's raw `SeasonalityResult` into the readable table, honest
interpretive summary, and CSV export the milestone promises — "loud and honest,
not just compliant."
Output: Four new functions + two module constants in `scanner/seasonality.py`,
covered by unit tests in `tests/test_seasonality.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cli-output-reporting/07-CONTEXT.md
@.planning/phases/07-cli-output-reporting/07-PATTERNS.md
@scanner/seasonality.py
@scanner/report.py
@tests/test_seasonality.py
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols in `scanner/seasonality.py`:
- `pad_weeks_table(result: SeasonalityResult) -> pd.DataFrame` — 52-row display DataFrame (name at executor discretion per D-11)
- `render_weeks_table(padded: pd.DataFrame) -> str` — plain-text table string
- `build_summary(result: SeasonalityResult) -> str` — interpretive summary text block
- `write_weeks_csv(padded: pd.DataFrame, path: str | Path) -> None` — CSV writer (the one deliberate I/O helper in this module)
- Module-level constant holding the static survivorship-bias warning text (D-09/D-10)
- Module-level constant holding the multiple-comparison caveat text (D-05)

New tests in `tests/test_seasonality.py` for each of the four functions.
No new files, no new dependencies (pandas already a project dependency), no schema change.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pad_weeks_table + render_weeks_table (D-01..D-04, D-12)</name>
  <files>scanner/seasonality.py, tests/test_seasonality.py</files>
  <read_first>
    - .planning/phases/07-cli-output-reporting/07-CONTEXT.md (D-01, D-02, D-03, D-04, D-11, D-12)
    - .planning/phases/07-cli-output-reporting/07-PATTERNS.md (to_string(index=False) idiom; None/pd.isna guard-before-format pattern)
    - scanner/seasonality.py lines 67-81 (SeasonalityResult) and 422-478 (compute_seasonality_stats — the exact `weeks` column set: week, mean_daily_ret_bps, delta_vs_baseline_bps, ci_low_bps, ci_high_bps, median_bps, n_obs, n_years, significant, insufficient_years, std_bps)
    - scanner/seasonality.py lines 316-419 (bootstrap_week_ci CR-01 docstring — what insufficient_years means)
  </read_first>
  <behavior>
    - Test: pad_weeks_table on a result whose weeks holds only weeks {1, 2} returns a DataFrame of exactly 52 rows with `week` values 1..52 ascending and exactly these 9 columns in order: week, mean_daily_ret_bps, delta_vs_baseline_bps, ci_low_bps, ci_high_bps, median_bps, n_obs, n_years, significant (D-11).
    - Test: for a week absent from result.weeks (e.g. week 3), every numeric cell (mean_daily_ret_bps, delta_vs_baseline_bps, ci_low_bps, ci_high_bps, median_bps, n_obs, n_years) equals the string "N/A" and `significant` is False (D-01).
    - Test: for a present week with insufficient_years=True, ci_low_bps and ci_high_bps equal "N/A" while mean_daily_ret_bps/delta_vs_baseline_bps/median_bps still show formatted numbers, and significant is False (D-02).
    - Test: a present, computable week renders its five bps columns as strings with exactly 2 decimal places (e.g. -12.40, not -12.4 or -12.400) (D-03).
    - Test: render_weeks_table(pad_weeks_table(result)) returns a str containing all 9 column headers and 52 data lines, produced through DataFrame.to_string(index=False) (D-04); the returned string contains the "N/A" sentinel for the padded week.
  </behavior>
  <action>
    Add `pad_weeks_table(result)` to scanner/seasonality.py. Reindex result.weeks onto the full week range 1..52. Build the single shared display DataFrame with the 9 SEAS-10 columns in the D-11 order (week, mean_daily_ret_bps, delta_vs_baseline_bps, ci_low_bps, ci_high_bps, median_bps, n_obs, n_years, significant). Apply the display rules: (a) weeks with no source row -> "N/A" in all seven numeric columns and significant False (D-01); (b) present weeks with insufficient_years True -> ci_low_bps/ci_high_bps become "N/A" while the other bps columns keep their formatted value, significant already False from Phase 6 (D-02); (c) the five bps columns (mean_daily_ret_bps, delta_vs_baseline_bps, ci_low_bps, ci_high_bps, median_bps) format present values to exactly 2 decimals as strings (D-03); (d) n_obs/n_years present -> int, missing -> "N/A"; (e) significant -> Python bool. Guard every cell format with an explicit `pd.isna`/`None` check (never a bare truthy test) per the 07-PATTERNS.md pitfall note. Drop the `insufficient_years` and `std_bps` columns from the output — they are not SEAS-10 display columns (D-02, D-11). Add `render_weeks_table(padded)` that column-orders (defensively) and returns `padded.to_string(index=False)` (D-04). Do NOT compute any new statistics; this is pure formatting of Phase 6 output. Document both functions with the module's docstring convention, citing D-01..D-04, D-11, D-12 and SEAS-10 inline.
  </action>
  <verify>
    <automated>pytest -q tests/test_seasonality.py</automated>
  </verify>
  <done>pad_weeks_table returns a 52-row, 9-column display DataFrame honoring D-01/D-02/D-03/D-11; render_weeks_table returns the to_string(index=False) rendering; new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_summary + warning/caveat constants (D-05..D-10)</name>
  <files>scanner/seasonality.py, tests/test_seasonality.py</files>
  <read_first>
    - .planning/phases/07-cli-output-reporting/07-CONTEXT.md (D-05, D-06, D-07, D-08, D-09, D-10; and the <specifics> "default to MORE explanatory text" guidance)
    - .planning/phases/07-cli-output-reporting/07-PATTERNS.md ("accumulate lines, join once" summary idiom; per-item formatted-string list; static warning-text constant analog)
    - scanner/report.py lines 37-47 (_BIAS_SURVIVORSHIP constant style) and 612-799 (render_report line-accumulation pattern)
    - scanner/seasonality.py lines 67-81 (SeasonalityResult fields: baseline_mean_bps, n_years, weeks)
  </read_first>
  <behavior>
    - Test: a module-level survivorship-warning constant is a fixed one-line string (identical every call, no interpolation) whose text mentions "today's sector membership", "historical prices", that delisted/removed tickers are excluded, and that this can "inflate apparent seasonality" (D-09, D-10).
    - Test: a module-level multiple-comparison caveat constant mentions 52 tests, ~5% significance, ~2-3 false positives expected by chance, and advises treating a single flagged week with caution (D-05).
    - Test: build_summary(result) with two significant weeks returns text containing one line per significant week in the form "Week 28: -12.40 bps (CI: -18.20 to -3.10)" — week number, delta, and CI all present (D-06).
    - Test: build_summary(result) with zero significant weeks contains the exact substring "none — no week deviates significantly from baseline" (D-06/SEAS-11).
    - Test: build_summary always contains a top-5-highest and a bottom-5-lowest-by-delta section even when zero weeks are significant (D-07); with a result of only 3 distinct weeks it shows min(5, available) per list and never lists the same week in both highest and lowest (D-08).
    - Test: build_summary lists the baseline mean (result.baseline_mean_bps) and the multiple-comparison caveat text (D-05); when a week has insufficient_years=True it appears in an explicit "uncomputable CI" callout (D-02).
  </behavior>
  <action>
    Define two module-level constants in scanner/seasonality.py in the `_`-prefixed static-string style of scanner/report.py's `_BIAS_SURVIVORSHIP`: one for the survivorship-bias warning (D-09/D-10 — static, non-interpolated, one line; must state that the tool applies TODAY's sector membership to each ticker's full historical price series, excludes delisted/removed tickers, and that this can inflate apparent seasonality) and one for the multiple-comparison caveat (D-05 — spell out the math: ~52 independent tests at ~5% significance means ~2-3 false positives expected by chance alone, so treat any single flagged week with caution). Add `build_summary(result)` that accumulates a `list[str]` and joins once (report.py idiom). It must include, from result: the baseline mean (result.baseline_mean_bps, 2 decimals); the significant-weeks list built by iterating result.weeks rows where significant is True, each formatted as `Week {week}: {delta_vs_baseline_bps:.2f} bps (CI: {ci_low_bps:.2f} to {ci_high_bps:.2f})` (D-06), or the literal line `none — no week deviates significantly from baseline` when none qualify; the top-5-highest and bottom-5-lowest weeks by delta_vs_baseline_bps, always printed, using min(5, available) and de-duplicating any week that would appear in both lists when fewer than 10 distinct weeks exist (D-07, D-08); an explicit callout naming any weeks with insufficient_years True as having an uncomputable CI so a reader never confuses it with "tested and not significant" (D-02); and the multiple-comparison caveat constant (D-05). Prefer more explanatory phrasing over terse per the <specifics> guidance. Use `pd.isna` guards before formatting any optional numeric. Do NOT print the survivorship warning here — it is the header line printed by main in Plan 02; just expose the constant. Document build_summary citing D-05..D-09 and SEAS-11/SEAS-12 inline.
  </action>
  <verify>
    <automated>pytest -q tests/test_seasonality.py</automated>
  </verify>
  <done>build_summary returns explanatory text with baseline, significant-weeks list (or none-message), top/bottom-5, insufficient_years callout, and the caveat; the two static constants exist with the required content; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: write_weeks_csv (D-11, D-12, D-13)</name>
  <files>scanner/seasonality.py, tests/test_seasonality.py</files>
  <read_first>
    - .planning/phases/07-cli-output-reporting/07-CONTEXT.md (D-11, D-12, D-13)
    - .planning/phases/07-cli-output-reporting/07-PATTERNS.md (CSV export + Path().mkdir(parents=True, exist_ok=True) analog from scan.py:158-160 and scan.py:282-283)
    - scanner/seasonality.py (existing ValueError-with-message convention: resolve_sector, check_thin_data)
  </read_first>
  <behavior>
    - Test: write_weeks_csv(padded, tmp_path/"sub/dir/out.csv") creates the missing parent directories and writes the file (D-13 — Path.mkdir(parents=True, exist_ok=True)).
    - Test: the written CSV has exactly 52 data rows plus a header, and its columns are exactly the 9 SEAS-10 display columns in order (week, mean_daily_ret_bps, delta_vs_baseline_bps, ci_low_bps, ci_high_bps, median_bps, n_obs, n_years, significant) with no insufficient_years column (D-11).
    - Test: the CSV cell contents match the shared padded DataFrame exactly, including "N/A" cells (D-12 — stdout and CSV never disagree).
    - Test: writing to a path whose file already exists overwrites it silently without raising (D-13).
  </behavior>
  <action>
    Add `write_weeks_csv(padded, path)` to scanner/seasonality.py. It takes the SAME padded DataFrame produced by pad_weeks_table (D-12 — one shared, already-padded DataFrame feeds both stdout render and CSV, so the two never disagree on row count or content) and writes it with `DataFrame.to_csv(path, index=False)`. Before writing, resolve `Path(path).parent` and call `mkdir(parents=True, exist_ok=True)` (D-13 — mirrors scan.py:282-283). Overwrite an existing target silently (default to_csv behavior — D-13). Do not add any column beyond the 9 SEAS-10 display columns (D-11). This is the module's one deliberate file-I/O helper (the thin CLI must not inline CSV logic, per 07-PATTERNS.md line 151); if a genuinely unwritable path surfaces after mkdir, let it raise so Plan 02's `try/except ValueError` / error path can surface it — do not swallow. Document write_weeks_csv citing D-11, D-12, D-13 and SEAS-13 inline.
  </action>
  <verify>
    <automated>pytest -q tests/test_seasonality.py</automated>
  </verify>
  <done>write_weeks_csv auto-creates parent dirs, writes the 52-row 9-column padded table identical to the stdout content, and overwrites silently; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg → in-memory transform | `--output` path string enters the module; all table/summary logic operates on trusted in-process `SeasonalityResult` data (no external/network input in Phase 7) |
| module → local filesystem | `write_weeks_csv` writes a user-specified local path (only crossing point that touches the filesystem) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | write_weeks_csv `--output` path (traversal / overwrite of an arbitrary local file) | low | accept | Locked decision D-13: auto-create parent dirs and overwrite silently is the intended UX for a single-user local diagnostic tool; the operator supplies and controls their own path. Not a gap to "fix." |
| T-07-02 | Information disclosure | CSV file contents at user-chosen path | low | accept | Output is derived sector-level aggregate statistics (no secrets/PII); path is operator-controlled on their own machine. |
| T-07-SC | Tampering | package installs | low | accept | No new dependencies installed this phase — pandas is already a project dependency; no npm/pip/cargo install task exists, so no supply-chain surface is added. |
</threat_model>

<verification>
- `pytest -q tests/test_seasonality.py` is green (new Phase 7 unit tests plus all existing Phase 5/6 tests).
- pad_weeks_table output has exactly 52 rows and 9 columns; missing weeks and insufficient_years ci cells show "N/A".
- build_summary text contains baseline, significant-weeks list (or none-message), top/bottom-5, insufficient_years callout, and the caveat.
- write_weeks_csv produces a 52-row CSV whose content matches the padded DataFrame including "N/A" cells.
- No `yf.` import added; no `datetime.now()`/`pd.Timestamp.now()` in the new functions (pure formatting).
</verification>

<success_criteria>
All four Phase 7 functions and the two static constants exist in `scanner/seasonality.py`, honoring D-01..D-13, and `pytest -q` stays green. The functions are pure/testable without argparse, ready for Plan 02 to wire into the CLI.
</success_criteria>

<output>
Create `.planning/phases/07-cli-output-reporting/07-01-SUMMARY.md` when done.
</output>
