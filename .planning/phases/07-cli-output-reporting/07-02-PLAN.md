---
phase: 07-cli-output-reporting
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - seasonality_by_week.py
  - tests/test_seasonality_cli.py
autonomous: true
requirements: [SEAS-10, SEAS-11, SEAS-12, SEAS-13]
must_haves:
  truths:
    - "Running seasonality_by_week.py prints, in order: the survivorship-bias warning header, the 52-row per-week table, and the interpretive summary (SEAS-10, SEAS-11, SEAS-12)"
    - "Passing --output writes the padded table to CSV AND still prints the full stdout output (D-14, SEAS-13)"
    - "Omitting --output prints stdout output and writes no file (SEAS-13)"
    - "main stays thin — it composes scanner.seasonality calls plus prints/file-write, with no rendering/padding/CSV logic inline (Phase 5/6 thin-CLI convention)"
  artifacts:
    - "seasonality_by_week.py — main wired to pad_weeks_table/render_weeks_table/build_summary/write_weeks_csv and the survivorship-warning header; --output help text updated to reflect Phase 7 activation"
    - "tests/test_seasonality_cli.py — CLI tests for header/table/summary output and --output CSV behavior"
  key_links:
    - "main calls compute_seasonality_stats (Phase 6) then pad_weeks_table once and feeds that single padded DataFrame to both render_weeks_table (stdout) and write_weeks_csv (--output), guaranteeing stdout and CSV agree (D-12)"
---

<objective>
Wire the Plan 01 presentation functions into `seasonality_by_week.py::main`: print
the survivorship-bias warning header, the 52-row table, and the interpretive
summary to stdout on every run, and additionally write the padded table to CSV
when `--output` is given. Replace the Phase 6 placeholder summary print (lines
82-87) and activate the previously-unused `--output` argument.

Purpose: Deliver the user-facing SEAS-10..13 output — the whole point of Phase 7 —
through the existing thin-CLI-delegates-to-`scanner`-module shape.
Output: Updated `main` in `seasonality_by_week.py`, covered by CLI tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cli-output-reporting/07-CONTEXT.md
@.planning/phases/07-cli-output-reporting/07-PATTERNS.md
@seasonality_by_week.py
@tests/test_seasonality_cli.py
@.planning/phases/07-cli-output-reporting/07-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

Modified `seasonality_by_week.py`:
- `main()` — new call sites: prints the survivorship-warning header constant, `render_weeks_table(pad_weeks_table(result))`, and `build_summary(result)`; conditionally calls `write_weeks_csv(...)` when `args.output` is truthy, with a "Saved → {path}" confirmation line
- `--output` argument help text updated (no longer "not used yet")
- Phase 6 placeholder summary print (current lines 82-87) removed/replaced

New/updated tests in `tests/test_seasonality_cli.py` for header + table + summary stdout and for `--output` CSV write (with and without `--output`).
No new files, no new dependencies, no schema change.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Wire header + table + summary + CSV into main (D-12, D-14, SEAS-10..13)</name>
  <files>seasonality_by_week.py</files>
  <read_first>
    - .planning/phases/07-cli-output-reporting/07-CONTEXT.md (D-12, D-14; <integration_points> — Phase 7 code called after compute_seasonality_stats returns)
    - .planning/phases/07-cli-output-reporting/07-PATTERNS.md (seasonality_by_week.py::main section — placeholder to replace lines 74-87, --output placeholder lines 45-48, thin-CLI shape to maintain, ValueError-catch-exit-2 convention to preserve)
    - seasonality_by_week.py (whole file — current main, argparse, try/except ValueError block)
    - .planning/phases/07-cli-output-reporting/07-01-SUMMARY.md (exact function/constant names Plan 01 created)
  </read_first>
  <action>
    In seasonality_by_week.py::main, after `compute_seasonality_stats` returns `result` (inside the existing success path, keeping the existing `Sector:`/`Admitted:`/`Skipped:` context prints and the `try/except ValueError -> stderr, return 2` block intact): (1) print the Plan 01 survivorship-warning module constant as the output header line, before the table (SEAS-12, D-09/D-10); (2) call `pad_weeks_table(result)` exactly ONCE into a local `padded` variable, then print `render_weeks_table(padded)` (SEAS-10, D-04); (3) print `build_summary(result)` (SEAS-11); (4) only if `args.output` is truthy, call `write_weeks_csv(padded, args.output)` — reusing the SAME `padded` DataFrame so stdout and CSV can never disagree (D-12) — then print a confirmation line such as `Saved → {args.output}` (mirroring scan.py's `if getattr(args, "csv", None): ...; print(f"\nSaved → {args.csv}")` idiom). stdout printing (header/table/summary) happens UNCONDITIONALLY regardless of `--output` (D-14/SEAS-13) — `--output` is additive, never a stdout replacement. Remove the Phase 6 placeholder summary print (current lines 82-87: the `Baseline:`/`Significant weeks:` lines) since build_summary now covers that content; keep the `Sector`/`Admitted`/`Skipped` data-readiness lines. Update the `--output` argparse help string (lines 45-48) to describe its Phase 7 behavior (write the results table to CSV) instead of "consumed in a later phase; not used yet". Keep main thin: only scanner.seasonality calls plus prints and the single conditional CSV write — no padding/formatting/CSV logic inline (07-PATTERNS.md line 151). If write_weeks_csv raises ValueError on a genuinely unwritable path, let the existing `except ValueError` block surface it to stderr and return 2 (extend that block's scope rather than adding a second catch).
  </action>
  <verify>
    <automated>python seasonality_by_week.py --help</automated>
  </verify>
  <done>main prints the survivorship header, the 52-row table, and the summary on every run, writes CSV only when --output is passed (reusing the single padded DataFrame), and the --output help text reflects Phase 7 activation; --help exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI tests for header/table/summary/CSV + full pytest green (SEAS-10..13)</name>
  <files>tests/test_seasonality_cli.py</files>
  <read_first>
    - tests/test_seasonality_cli.py (existing _fake_result / _fake_load / _fake_compute monkeypatch harness — reuse it)
    - .planning/phases/07-cli-output-reporting/07-CONTEXT.md (D-12, D-14, SEAS-10..13 expected output content)
    - seasonality_by_week.py (final wired main from Task 1)
  </read_first>
  <behavior>
    - Test: happy-path run (reusing the existing monkeypatch harness) prints stdout containing the survivorship-warning header text (a substring unique to the warning, e.g. "Survivorship") (SEAS-12).
    - Test: stdout contains a 52-row table — assert the header column names (e.g. "week", "significant") are present and that week 52 appears (padding to 52 rows, SEAS-10). Extend `_fake_result`'s weeks fixture so the assertions exercise the padded output (e.g. only weeks 1-2 present -> 52 rows rendered).
    - Test: stdout contains the summary — assert either a "Week N:" significant line or the "none — no week deviates significantly from baseline" substring, plus a caveat substring (SEAS-11).
    - Test: with `--output <tmp_path/out.csv>`, main returns 0, the CSV file exists with 52 data rows and the 9 SEAS-10 columns, AND stdout still contains the table/summary (D-14/SEAS-13 — additive, not a replacement).
    - Test: without `--output`, no CSV file is written and stdout still contains the table (SEAS-13).
    - Test: a run whose `--output` parent directory does not exist still succeeds and creates it (D-13) — via a tmp_path subdirectory that doesn't pre-exist.
  </behavior>
  <action>
    Extend tests/test_seasonality_cli.py using the existing `_fake_result`/`_fake_load`/`_fake_compute` monkeypatch pattern (do not hit yfinance or the real pipeline). Update `_fake_result`'s `weeks` fixture as needed so it carries the `insufficient_years` column (Phase 6's real output has it) and only a couple of present weeks, so tests exercise both the 52-row padding and the N/A cells. Add tests asserting: the survivorship header substring appears in stdout (SEAS-12); the rendered table has 52 rows and the 9 SEAS-10 column headers (SEAS-10); the summary block content (significant list or none-message, plus caveat) appears (SEAS-11); `--output tmp.csv` writes a 52-row 9-column CSV while stdout still prints the table (D-14/SEAS-13); omitting `--output` writes no file; a non-existent `--output` parent dir is auto-created (D-13). Use `tmp_path` and `capsys`. Keep the existing tests (invalid sector exits 2, thin-data ValueError exits 2, default universe sp500) passing — adjust their assertions only if the removed placeholder `Significant weeks: N of M` line was asserted (it was, in test_main_happy_path_prints_summary — update that assertion to the new summary content).
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <done>All CLI tests pass and the full `pytest -q` suite is green, proving SEAS-10..13 behavior end-to-end through the CLI with no regressions to Phase 5/6 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → main | `--sector`/`--universe`/`--output` parsed by argparse; only `--output` reaches the filesystem, via Plan 01's write_weeks_csv |
| stdout / local file | main prints to stdout unconditionally and writes one operator-specified CSV when `--output` is given |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | `--output` path passed to write_weeks_csv (traversal / overwrite) | low | accept | Locked decision D-13 (auto-create + silent overwrite) is the intended UX for a single-user local diagnostic CLI; the operator controls their own path. Same disposition as T-07-01. |
| T-07-04 | Denial of Service | unhandled exception from an unwritable `--output` path | low | mitigate | write_weeks_csv raises ValueError on a genuinely unwritable path after mkdir; main's existing `except ValueError -> stderr, return 2` block surfaces it cleanly instead of a raw traceback (Task 1). |
| T-07-SC | Tampering | package installs | low | accept | No new dependencies this phase — no npm/pip/cargo install task; no supply-chain surface added. |
</threat_model>

<verification>
- `python seasonality_by_week.py --help` exits 0 and the `--output` help text reflects Phase 7 activation.
- `pytest -q` is fully green (new CLI tests + all Phase 5/6/7 tests, no regressions).
- stdout on a run contains, in order: survivorship warning header, 52-row table, interpretive summary.
- `--output` writes a 52-row CSV while stdout still prints; omitting `--output` writes no file.
- main contains no padding/formatting/CSV logic inline (thin-CLI convention preserved).
- No `yf.` import added; no `datetime.now()` in main.
</verification>

<success_criteria>
`seasonality_by_week.py` prints the survivorship warning, the 52-row table, and the
interpretive summary on every run, and writes the identical padded table to CSV when
`--output` is given (D-14/SEAS-13). The full `pytest -q` suite stays green, and main
remains a thin orchestrator over `scanner.seasonality`.
</success_criteria>

<output>
Create `.planning/phases/07-cli-output-reporting/07-02-SUMMARY.md` when done.
</output>
