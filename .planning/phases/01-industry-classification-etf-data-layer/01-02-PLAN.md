---
phase: 01-industry-classification-etf-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scanner/core.py
  - tests/test_core.py
autonomous: true
requirements: [IND-01]
must_haves:
  truths:
    - "QualityInfo constructed with the 5 existing positional args has industry is None and industry_key is None (D-04)"
    - "_make_quality_info() populates industry from info['industry'] and industry_key from info['industryKey'] off the same info dict (D-04)"
    - "A ticker with no industry classification yields QualityInfo.industry is None (not an empty string)"
  artifacts:
    - "scanner/core.py — QualityInfo.industry and QualityInfo.industry_key fields; _make_quality_info() fetch"
    - "tests/test_core.py — QualityInfo None-default test"
  key_links:
    - "_make_quality_info reads info.get('industry') / info.get('industryKey') from the already-fetched info dict — no extra yfinance call (D-04)"
    - "New fields are appended last with None defaults so existing 5-positional-arg QualityInfo callsites stay valid (D-04, D-05)"
---

<objective>
Give the pipeline industry classification: add industry and industry_key (both Optional[str], default None) to the frozen QualityInfo dataclass (D-04, D-05) and populate them in _make_quality_info() from the already-fetched yfinance info dict (D-04). Delivers IND-01 — every signal can show its industry group name.

Purpose: This is the classification slice that makes resolve_industry_etf() (Plan 01) usable per-ticker in Phase 2. industry is the human-readable display name; industry_key is the slug fed to resolve_industry_etf().
Output: Two new QualityInfo fields, the _make_quality_info() fetch, and an offline test pinning the None default.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-industry-classification-etf-data-layer/01-CONTEXT.md
@.claude/CLAUDE.md
@scanner/core.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add industry and industry_key fields to QualityInfo with a None-default test</name>
  <files>scanner/core.py, tests/test_core.py</files>
  <read_first>
    - scanner/core.py — QualityInfo @dataclass(frozen=True) at line 135 (5 current fields: profitable, market_cap, debt_equity, sector, float_shares)
    - scanner/backtest.py — lines 261-267 and the QualityInfo(False, None, None, None, None) construction at line 335 (5-positional callsite that must remain valid)
    - tests/test_core.py — line 166 QualityInfo(True, 2.5e9, 50.0, "Technology", 50e6) positional construction; tests/test_strategies.py line 35, tests/test_targets.py lines 95/116/131, tests/test_fixtures.py line 28 (other construction styles — confirm appended defaults keep them all valid)
  </read_first>
  <behavior>
    - QualityInfo(False, None, None, None, None).industry is None
    - QualityInfo(False, None, None, None, None).industry_key is None
    - QualityInfo(profitable=True, market_cap=2.5e9, debt_equity=50.0, sector="Technology", float_shares=50e6, industry="Semiconductors", industry_key="semiconductors").industry == "Semiconductors"
  </behavior>
  <action>
    Append two fields to the QualityInfo frozen dataclass, AFTER the existing five fields so the new fields are last and every existing 5-positional-arg construction (backtest.py:335, test_core.py:166, etc.) stays valid: industry: Optional[str] = None and industry_key: Optional[str] = None (D-04). Both default to None (D-04). Use the bare field names industry and industry_key — no _name suffix — matching the existing sector convention (D-05). Either `= None` or field(default=None) is acceptable per CONTEXT discretion; the existing dataclass uses plain annotations so prefer `= None`.

    Add a test to tests/test_core.py, selectable with -k industry_default, asserting both new fields default to None when QualityInfo is built with the five legacy positional args, and that they round-trip when passed explicitly (per the behavior block). No network — construct QualityInfo directly. This satisfies success criterion 3 (no classification -> NULL/None, not empty string).
  </action>
  <verify>
    <automated>cd D:/Projects/TraderAssist && python -m pytest tests/test_core.py -k industry_default -q</automated>
  </verify>
  <acceptance_criteria>
    - QualityInfo has fields industry: Optional[str] = None and industry_key: Optional[str] = None appended after float_shares (D-04, D-05)
    - QualityInfo(False, None, None, None, None) constructs without error and both new fields are None (success criterion 3)
    - `python -m pytest -q` stays green offline — all existing positional/keyword QualityInfo callsites (backtest.py, test_core.py, test_strategies.py, test_targets.py, test_fixtures.py, test_golden_master.py) still construct correctly
  </acceptance_criteria>
  <done>QualityInfo carries industry and industry_key defaulting to None; the None-default test passes; no existing callsite breaks.</done>
</task>

<task type="auto">
  <name>Task 2: Populate industry and industry_key in _make_quality_info()</name>
  <files>scanner/core.py</files>
  <read_first>
    - scanner/core.py — _make_quality_info() at lines 346-381 (the info dict fetch with the 3-retry rate-limit loop, the info.get("sector") extraction block, and the final QualityInfo(...) return)
    - .claude/CLAUDE.md — "Version Compatibility" table (info['industryKey'] available from yfinance 0.2.18+) and the "Implementation Checklist" line "Extend _make_quality_info() to extract info.get('industry') and info.get('industryKey')"
  </read_first>
  <action>
    In _make_quality_info(), after the existing sector = info.get("sector") extraction, add industry = info.get("industry") and industry_key = info.get("industryKey") read from the SAME already-fetched info dict (D-04) — do not add any new yf.Ticker(...).info call or extra network round-trip. Pass both into the QualityInfo(...) return as industry=industry and industry_key=industry_key. When info is empty (rate-limited / unfetchable, the existing empty-dict path), info.get(...) yields None, so both fields default to None — the same SKIP-on-absence philosophy as the other quality fields; do not substitute an empty string. Note yfinance returns the slug under the key 'industryKey' (capital K), distinct from the display key 'industry'.
  </action>
  <verify>
    <automated>cd D:/Projects/TraderAssist && python -m pytest tests/test_core.py -q</automated>
    <human-check>Run `python scan.py scan --strategy pullback --ticker AAPL` (or another live ticker) and any of ~10 representative tickers across sectors; confirm yfinance .info['industry'] returns a non-null human-readable string (e.g. "Semiconductors", "Software—Infrastructure") whose corresponding industryKey matches a key in INDUSTRY_ETF_MAP — success criterion 1. Network-dependent; verified at end of phase. (IND-01)</human-check>
  </verify>
  <acceptance_criteria>
    - _make_quality_info() reads info.get("industry") and info.get("industryKey") from the existing info dict with no added network call (D-04)
    - The QualityInfo(...) return passes industry= and industry_key= through
    - When info is empty, both resolve to None (not "")
    - `python -m pytest -q` stays green offline
    - End-of-phase human-check: .info['industry'] returns non-null strings matching INDUSTRY_ETF_MAP keys for 10 representative tickers (success criterion 1, IND-01)
  </acceptance_criteria>
  <done>_make_quality_info() populates both new fields from the cached info dict; offline suite green; live tickers return industry strings matching the map.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces
- `QualityInfo.industry: Optional[str] = None` — display name field on the frozen dataclass in scanner/core.py (D-04, D-05)
- `QualityInfo.industry_key: Optional[str] = None` — slug field for ETF lookup on the frozen dataclass in scanner/core.py (D-04, D-05)
- `_make_quality_info()` extended to extract info.get('industry') and info.get('industryKey') (D-04)
- New QualityInfo None-default test in tests/test_core.py (selectable via `-k industry_default`)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yfinance .info dict -> _make_quality_info() | Untrusted external classification strings (industry, industryKey) enter QualityInfo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | _make_quality_info() reading info['industry'] / info['industryKey'] | low | mitigate | Values are stored as opaque Optional[str] and only ever used as dict keys (resolve_industry_etf) or display text — never eval'd, path-joined, or used in SQL in this phase; missing/garbage classification resolves to None via the D-06/D-07 chain, no crash |
| T-02-02 | Denial of Service | yfinance rate-limiting on the info fetch | low | accept | No new network call added — fields read from the same info dict already fetched under the existing 3-retry rate-limit loop |

No package installs in this plan — T-02-SC supply-chain gate not applicable.
</threat_model>

<verification>
- `python -m pytest -q` is green offline (full suite, no regressions)
- QualityInfo None-default test passes (success criterion 3)
- End-of-phase human-check: live tickers return industry strings matching INDUSTRY_ETF_MAP keys (success criterion 1, IND-01)
</verification>

<success_criteria>
- QualityInfo carries industry and industry_key (Optional[str], default None), populated from the yfinance info dict (D-04, D-05, IND-01)
- A ticker with no classification yields None (not empty string), proven by the offline test (success criterion 3)
- No existing QualityInfo callsite breaks; offline suite green
</success_criteria>

<output>
Create `.planning/phases/01-industry-classification-etf-data-layer/01-02-SUMMARY.md` when done
</output>
