---
phase: 01-industry-classification-etf-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scanner/core.py
  - scanner/data_store.py
  - tests/test_core.py
autonomous: true
requirements: [IND-01]
must_haves:
  truths:
    - "resolve_industry_etf('semiconductors', 'Technology') returns 'XSD' (D-01, D-07)"
    - "resolve_industry_etf(None, 'Technology') returns None with no sector fallback (D-06)"
    - "resolve_industry_etf('unmapped-key', 'Technology') returns 'XLK' via SECTOR_ETF_MAP fallback (D-07)"
    - "Every unique ETF value in INDUSTRY_ETF_MAP appears exactly once in _MARKET_SYMBOLS; no existing sector ETF duplicated (D-02)"
  artifacts:
    - "scanner/core.py — INDUSTRY_ETF_MAP dict + resolve_industry_etf() helper"
    - "scanner/data_store.py — extended _MARKET_SYMBOLS list"
    - "tests/test_core.py — resolve_industry_etf chain tests"
  key_links:
    - "resolve_industry_etf falls through INDUSTRY_ETF_MAP.get(industry_key) -> SECTOR_ETF_MAP.get(sector) -> None (D-07)"
    - "_MARKET_SYMBOLS is the deduplicated union of existing sector ETFs and all INDUSTRY_ETF_MAP values (D-02)"
---

<objective>
Build the industry-ETF resolution data layer: the full INDUSTRY_ETF_MAP (D-01, D-03), a unit-testable resolve_industry_etf() helper implementing the two-step lookup chain (D-06, D-07), and the deduplicated _MARKET_SYMBOLS extension so every covered industry ETF is Parquet-cached on the next refresh (D-02).

Purpose: This is the foundational data infrastructure. Phase 2 momentum computation consumes resolve_industry_etf() and the cached ETF price frames; nothing here computes momentum or touches the DB.
Output: INDUSTRY_ETF_MAP, resolve_industry_etf(), extended _MARKET_SYMBOLS, and offline resolver tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-industry-classification-etf-data-layer/01-CONTEXT.md
@.claude/CLAUDE.md
@scanner/core.py
@scanner/data_store.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define INDUSTRY_ETF_MAP and resolve_industry_etf() in core.py</name>
  <files>scanner/core.py, tests/test_core.py</files>
  <read_first>
    - scanner/core.py — SECTOR_ETF_MAP at line 51 (exact dict pattern to mirror; place INDUSTRY_ETF_MAP adjacent, directly after it) and _sector_strength() at line 215 (the SECTOR_ETF_MAP.get / market_data.get lookup pattern)
    - .claude/CLAUDE.md — the "Industry ETF Proxy Map" section with all sector subsections (Technology, Healthcare, Financial Services, Consumer Cyclical, Industrials, Energy, Basic Materials) and the "What NOT to Use" table (do not use yf.Industry().ticker, XNTK, or the IBD 252-day formula)
    - tests/test_core.py — existing test structure, imports (core, QualityInfo), and assertion style to mirror for the new resolver tests
  </read_first>
  <behavior>
    - resolve_industry_etf('semiconductors', 'Technology') == 'XSD' (direct map hit)
    - resolve_industry_etf('oil-gas-integrated', 'Energy') == 'XLE' (sector-fallback entry encoded in the map itself, per D-03)
    - resolve_industry_etf(None, 'Technology') is None (D-06: industry_key None returns None immediately, no sector fallback)
    - resolve_industry_etf('totally-unknown-key', 'Technology') == 'XLK' (D-07 step 2: key absent from map falls through to SECTOR_ETF_MAP.get(sector))
    - resolve_industry_etf('totally-unknown-key', None) is None (key absent and no sector to fall back to)
    - resolve_industry_etf('totally-unknown-key', 'Nonexistent Sector') is None (key absent, sector not in SECTOR_ETF_MAP)
  </behavior>
  <action>
    Add a module-level dict INDUSTRY_ETF_MAP in scanner/core.py immediately after SECTOR_ETF_MAP (line 51 region), mapping industryKey slug strings to ETF ticker strings. Copy the map VERBATIM from the "Industry ETF Proxy Map" subsections in .claude/CLAUDE.md — do not re-derive or abbreviate. The full map covers all sector subsections and contains roughly 34 industryKey entries resolving to 19 unique ETF tickers (XSD, XSW, XBI, XPH, XHE, XHS, KRE, KBE, KIE, KCE, XHB, XRT, XAR, XOP, XLE, XES, GDX, XLB, XME). Sector-level fallback entries such as 'oil-gas-integrated' -> 'XLE', 'oil-gas-midstream' -> 'XLE', and 'specialty-chemicals' -> 'XLB' are explicit entries inside this map (D-03) — the map is the single source of truth; do not create a separate fallback table.

    Add a named helper function resolve_industry_etf(industry_key, sector) implementing the two-step lookup chain (D-07). Signature: resolve_industry_etf(industry_key: Optional[str], sector: Optional[str]) -> Optional[str]. Behavior: if industry_key is None, return None immediately (D-06 — no sector fallback when classification is absent). Otherwise return INDUSTRY_ETF_MAP.get(industry_key); if that is None (key not present in the map at all), return SECTOR_ETF_MAP.get(sector); if that is also None, return None. Place the helper near the map and the _sector_strength() pattern. Make it a standalone module-level function (not a closure) so the test can import and call it directly.

    Add resolver tests to tests/test_core.py named so they are selectable with -k resolve_industry_etf, covering all six cases in the behavior block above. Import resolve_industry_etf from scanner.core. Do not add network calls — this function is pure.
  </action>
  <verify>
    <automated>cd D:/Projects/TraderAssist && python -m pytest tests/test_core.py -k resolve_industry_etf -q</automated>
  </verify>
  <acceptance_criteria>
    - INDUSTRY_ETF_MAP is defined in scanner/core.py with all entries from the .claude/CLAUDE.md proxy map (>= 30 keys), including the explicit sector-fallback entries 'oil-gas-integrated' -> 'XLE' and 'specialty-chemicals' -> 'XLB' (D-01, D-03)
    - resolve_industry_etf is a module-level function importable via `from scanner.core import resolve_industry_etf`
    - All six behavior cases pass under `pytest tests/test_core.py -k resolve_industry_etf` (covers success criterion 4: key-in-map resolves, key-not-in-map falls back to sector ETF, neither returns None)
    - `python -m pytest -q` stays green offline (no regressions)
  </acceptance_criteria>
  <done>resolve_industry_etf() implements the D-07 chain with the D-06 None short-circuit; INDUSTRY_ETF_MAP is the verbatim full research map; resolver tests pass offline.</done>
</task>

<task type="auto">
  <name>Task 2: Extend _MARKET_SYMBOLS with deduplicated industry ETFs</name>
  <files>scanner/data_store.py</files>
  <read_first>
    - scanner/data_store.py — _MARKET_SYMBOLS at lines 26-29 (current list: SPY plus the 11 sector ETFs) and fetch_with_retry at line 34 (retry wrapper used by the download path)
    - scanner/core.py — the INDUSTRY_ETF_MAP just defined in Task 1 (the source of the new ETF tickers)
    - tests/test_data_store.py — lines 262-291 (test_market_data_zero_fetches and test_market_data_end_alignment iterate ds._MARKET_SYMBOLS dynamically, so they self-adapt to the longer list; confirm no hardcoded length assumptions break)
  </read_first>
  <action>
    Extend the _MARKET_SYMBOLS list in scanner/data_store.py to be the deduplicated union of the existing entries (SPY + the 11 sector ETFs already present) and every unique ETF ticker value in INDUSTRY_ETF_MAP (D-02). Add the 17 new industry ETF tickers that are not already in the list: XSD, XSW, XBI, XPH, XHE, XHS, KRE, KBE, KIE, KCE, XHB, XRT, XAR, XOP, XES, GDX, XME. Do NOT add XLE or XLB — they are already in _MARKET_SYMBOLS as sector ETFs and must not be duplicated (D-02). No ticker may appear more than once in the final list. Do not import core into data_store to avoid a circular import; the new tickers are listed literally in _MARKET_SYMBOLS (data_store has no dependency on core). get_market_data() iterates this list, so the extension alone makes the new ETFs flow through the Parquet cache on the next refresh — no other change to data_store.py is needed (per CONTEXT code_context).
  </action>
  <verify>
    <automated>cd D:/Projects/TraderAssist && python -c "from scanner import data_store as ds; s=ds._MARKET_SYMBOLS; assert len(s)==len(set(s)), 'duplicate in _MARKET_SYMBOLS'; need={'XSD','XSW','XBI','XPH','XHE','XHS','KRE','KBE','KIE','KCE','XHB','XRT','XAR','XOP','XES','GDX','XME'}; assert need.issubset(set(s)), need-set(s); print('OK', len(s))" && python -m pytest tests/test_data_store.py -q</automated>
    <human-check>Run `python scan.py refresh --file universes/sample.txt` (or sp500) and confirm all 17 new industry ETF Parquet files appear under data/ohlcv/ (e.g. XSD.parquet, XBI.parquet, KRE.parquet) with no download errors logged — success criterion 2. Network-dependent; verified at end of phase.</human-check>
  </verify>
  <acceptance_criteria>
    - _MARKET_SYMBOLS contains no duplicates: len(_MARKET_SYMBOLS) == len(set(_MARKET_SYMBOLS)) (D-02)
    - All 17 new tickers (XSD, XSW, XBI, XPH, XHE, XHS, KRE, KBE, KIE, KCE, XHB, XRT, XAR, XOP, XES, GDX, XME) are present in _MARKET_SYMBOLS
    - XLE and XLB each appear exactly once (not re-added as industry entries)
    - tests/test_data_store.py passes (the list-driven market-data tests self-adapt to the longer list)
    - On `scan.py refresh`, a Parquet file is written for each new ETF under data/ohlcv/ with no download errors (success criterion 2 — human-check, end of phase)
  </acceptance_criteria>
  <done>_MARKET_SYMBOLS is the deduplicated union covering every INDUSTRY_ETF_MAP ETF; offline data_store tests pass; refresh caches the new ETFs.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces
- `INDUSTRY_ETF_MAP` — module-level dict in scanner/core.py (industryKey slug -> ETF ticker), ~34 entries / 19 unique ETFs (D-01, D-03)
- `resolve_industry_etf(industry_key: Optional[str], sector: Optional[str]) -> Optional[str]` — module-level helper in scanner/core.py (D-06, D-07)
- Extended `_MARKET_SYMBOLS` list in scanner/data_store.py — adds 17 industry ETF tickers, deduplicated (D-02)
- New resolver tests in tests/test_core.py (selectable via `-k resolve_industry_etf`)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yfinance API -> data_store Parquet cache | Untrusted external OHLCV downloaded for the 17 new ETF tickers crosses into the cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | yfinance ETF download for new tickers (_MARKET_SYMBOLS) | low | mitigate | Reuse existing fetch_with_retry + _MIN_ROWS (220) guard and tz-naive normalization already in data_store; malformed/short frames are rejected by the existing cache path, no new validation surface added |
| T-01-02 | Denial of Service | 17 extra ETF downloads per refresh increase rate-limit exposure | low | accept | Existing exponential-backoff retry in fetch_with_retry; downloads are cached so steady-state refreshes are incremental; acceptable for a single-user local tool |

No package installs in this plan (yfinance, pandas already installed) — T-01-SC supply-chain gate not applicable.
</threat_model>

<verification>
- `python -m pytest -q` is green offline (full suite, no regressions)
- resolve_industry_etf chain tests pass (success criterion 4)
- _MARKET_SYMBOLS is duplicate-free and contains all 17 new ETFs (D-02)
- End-of-phase human-check: `scan.py refresh` caches all new ETFs without error (success criterion 2)
</verification>

<success_criteria>
- INDUSTRY_ETF_MAP and resolve_industry_etf() exist in scanner/core.py and behave per the D-06/D-07 chain
- _MARKET_SYMBOLS is the deduplicated union including every INDUSTRY_ETF_MAP ETF (D-02)
- Offline test suite stays green; resolver tests cover all six cases
</success_criteria>

<output>
Create `.planning/phases/01-industry-classification-etf-data-layer/01-01-SUMMARY.md` when done
</output>
