---
phase: 03-industry-display-in-cli-web-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scan.py
  - tests/test_scan_display.py
autonomous: true
requirements: [IND-07]
must_haves:
  truths:
    - "Running `scan.py scan` prints the industry group name and 20-day momentum score in the qualified-setups table"
    - "A signal with NULL industry_momentum prints an em dash (—) in the CLI table, never 0 / 0.0 / None / empty"
    - "industry_above_50ma prints ↑ for 1, ↓ for 0 (integer zero is a valid 'below' state, not NULL)"
  artifacts:
    - "scan.py :: _print_scan_results() extended with Industry / Mom / Trend / Rank% columns"
    - "tests/test_scan_display.py :: offline capsys test asserting formatted CLI output"
  key_links:
    - "run_scan() df_out rows carry industry_group/industry_momentum/industry_above_50ma/industry_rank_pct (core.py:712-716) -> combined df -> _print_scan_results (scan.py:156)"
---

<objective>
Deliver the CLI half of IND-07: a trader running `scan.py scan` sees each qualified
signal's industry group name, 20-day momentum, 50-day-MA trend arrow, and rank percentile
in the results table — formatted per the locked decisions D-02..D-05 and the UI-SPEC CLI
section, with strict NULL handling (D-10).

Purpose: Surface the industry momentum data (already computed and stored in Phase 2) at the
point the trader reads scan output, so industry strength sits alongside score/confidence.
Output: An extended `_print_scan_results()` and a new offline test proving the formatting.

This is a complete vertical slice: after this plan a real user can run the scanner and read
industry momentum in the CLI — a capability that did not exist before.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-industry-display-in-cli-web-ui/03-CONTEXT.md
@.planning/phases/03-industry-display-in-cli-web-ui/03-PATTERNS.md
@.planning/phases/03-industry-display-in-cli-web-ui/03-UI-SPEC.md
</context>

<artifacts_produced>
## Artifacts this phase produces (CLI slice)

CLI column additions to `scan.py :: _print_scan_results()` — four formatted display columns
inserted after `confidence`, before `close`:

- `Industry` — from `industry_group` (str), NULL -> `—`
- `Mom` — from `industry_momentum` (REAL), formatted `+5.2%` / `-3.1%`, NULL/NaN -> `—`
- `Trend` — from `industry_above_50ma` (int 0/1), `1` -> `↑`, `0` -> `↓`, NULL -> `—`
- `Rank%` — from `industry_rank_pct` (REAL), formatted `Top 18%`, NULL/NaN -> `—`

New test module: `tests/test_scan_display.py` with an offline capsys test named
`test_print_scan_results_industry_columns`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Failing test for industry columns in CLI scan output (RED)</name>
  <files>tests/test_scan_display.py</files>
  <read_first>
    - scan.py:189-200 (current `_print_scan_results` — the function under test)
    - scan.py:22 (sys.stdout.reconfigure utf-8, so arrow symbols are safe)
    - tests/conftest.py (fixture/import conventions for the suite)
    - tests/test_report.py (example of an existing display/formatting test style)
    - .planning/phases/03-industry-display-in-cli-web-ui/03-UI-SPEC.md §"CLI Display Specification" (column names, order, format rules)
  </read_first>
  <action>
    Create tests/test_scan_display.py. Import `_print_scan_results` from `scan` and pandas.
    Write `test_print_scan_results_industry_columns(capsys)` that builds an in-memory
    DataFrame of three qualified rows (set column `qualified` = True on all) containing the
    columns ticker, strategy, score, confidence, industry_group, industry_momentum,
    industry_above_50ma, industry_rank_pct, close, suggested_stop, suggested_target,
    risk_reward. Use one positive-momentum row (industry_momentum 5.2, industry_above_50ma 1,
    industry_rank_pct 18, industry_group a long name over 20 chars), one negative row
    (industry_momentum -3.1, industry_above_50ma 0, industry_rank_pct 62), and one all-NULL
    industry row (all four industry fields None / float nan). Call `_print_scan_results(df)`,
    read `capsys.readouterr().out`, and assert: the four header tokens Industry, Mom, Trend,
    Rank% each appear; the substring `+5.2%` appears; the substring `-3.1%` appears; the arrow
    `↑` appears and `↓` appears; `Top 18%` appears; and for the NULL row the em dash `—`
    (U+2014) appears. Also assert the raw stored values do NOT leak as `0.0` or `None` text for
    the NULL row. Do NOT implement any scan.py change in this task — the test must FAIL now
    because the current function omits these columns. This is the failing end-to-end test that
    anchors the slice (MVP first-task rule).
  </action>
  <verify>
    <automated>python -m pytest tests/test_scan_display.py::test_print_scan_results_industry_columns -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_scan_display.py -x` runs and the new test FAILS (RED) with an assertion error about a missing header or formatted value (not a collection/import error)
    - The test constructs its DataFrame entirely in-memory — no yfinance, no DB, no filesystem reads (stays green-capable offline)
    - Test includes at least one row with all four industry fields NULL/NaN and asserts `—` renders for it
  </acceptance_criteria>
  <done>tests/test_scan_display.py exists; the industry-columns test fails for the right reason (feature not yet implemented) and is fully offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend _print_scan_results with formatted industry columns (GREEN)</name>
  <files>scan.py</files>
  <read_first>
    - scan.py:189-200 (the function to extend)
    - tests/test_scan_display.py (the assertions this task must satisfy — written in Task 1)
    - .planning/phases/03-industry-display-in-cli-web-ui/03-UI-SPEC.md §"CLI Display Specification" (D-02/D-03/D-04 format rules, D-05 order)
    - .planning/phases/03-industry-display-in-cli-web-ui/03-PATTERNS.md §"scan.py — extend _print_scan_results" (NULL-guard pattern)
  </read_first>
  <behavior>
    - Positive momentum 5.2 -> Mom cell `+5.2%`; negative -3.1 -> `-3.1%`
    - industry_above_50ma == 1 -> Trend `↑`; == 0 -> `↓`; None/NaN -> `—`
    - industry_rank_pct 18 -> Rank% `Top 18%` (integer, no decimal); None/NaN -> `—`
    - industry_group None/NaN -> Industry `—`; present -> printed as stored (truncation is a UI-only concern, not CLI)
    - Column order: ticker, strategy, score, confidence, Industry, Mom, Trend, Rank%, close, suggested_stop, suggested_target, risk_reward (per D-05)
  </behavior>
  <action>
    In `_print_scan_results`, operate on a copy of the qualified frame before printing. Build
    four formatted display columns using a NULL guard of the form value-is-not-None AND NOT
    pd.isna(value) (NEVER a bare truthy check, because integer 0 is a valid industry_above_50ma
    state per the CONTEXT specifics note): Industry from industry_group (fillna to em dash),
    Mom from industry_momentum via an f-string `{v:+.1f}%`, Trend from industry_above_50ma
    mapping 1 to up-arrow / 0 to down-arrow / else em dash, and Rank% from industry_rank_pct
    via `Top {v:.0f}%`. Each formatted column is only built when its source column exists in the
    frame (guard with an `in df.columns` check so the function stays robust if a source column
    is absent). Replace the `cols` list so the display order is ticker, strategy, score,
    confidence, Industry, Mom, Trend, Rank%, close, suggested_stop, suggested_target,
    risk_reward, keep the existing `display_cols = [c for c in cols if c in ...]` filter (now
    filtering against the copy that has the formatted columns), and print via
    to_string(index=False) as before. The em dash character is U+2014. Do not change the
    qualified-count header lines or any non-display behavior.
  </action>
  <verify>
    <automated>python -m pytest tests/test_scan_display.py -x && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_scan_display.py -x` PASSES (the Task 1 test is now GREEN)
    - `python -m pytest -q` is green (full suite offline — no regressions)
    - Grepping scan.py for the formatted columns confirms the order: `grep -n "Industry" scan.py` and `grep -n "Rank%" scan.py` show them inside `_print_scan_results` between the `confidence` and `close` entries of the display column list
    - The NULL row in the test renders `—` for all four industry cells (no `0`, `0.0`, `None`, or empty)
  </acceptance_criteria>
  <done>`_print_scan_results` prints Industry / Mom / Trend / Rank% between confidence and close with correct formatting and NULL handling; new test and full pytest suite pass offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataFrame -> stdout | Already-computed, already-persisted signal values are formatted into a terminal string. No new external input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `_print_scan_results` stdout | low | accept | Data printed (industry name/momentum) is already user-owned scan output; no secrets, no PII, no auth material involved. |
| T-03-02 | Tampering | industry source values | low | accept | Values originate from Phase 2 computation already in the frame; this plan only formats them for display and adds no write path. |

No user input, no network calls, no auth changes, no package installs in this plan — threat
surface is minimal and documented as accepted at ASVS Level 1.
</threat_model>

<verification>
- `python -m pytest tests/test_scan_display.py -x` passes
- `python -m pytest -q` passes offline (per CLAUDE.md gate)
- Manual smoke (optional, non-blocking): `python scan.py scan --strategy both --file universes/sample.txt` shows the four industry columns between Conf and Entry with — for any unmapped industry
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 1 met: `scan.py scan` prints industry group name and 20-day momentum alongside other signal fields
- NULL industry fields render `—` in CLI (criterion 3 CLI half; D-10)
- Full pytest suite green offline
</success_criteria>

<output>
Create `.planning/phases/03-industry-display-in-cli-web-ui/03-01-SUMMARY.md` when done
</output>
