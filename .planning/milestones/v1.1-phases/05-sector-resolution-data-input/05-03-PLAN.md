---
phase: 05-sector-resolution-data-input
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - seasonality_by_week.py
  - tests/test_seasonality_cli.py
autonomous: true
requirements: [SEAS-01, SEAS-02]
must_haves:
  truths:
    - "seasonality_by_week.py --sector Technology --universe sp500 prints the resolved sector, matched count, admitted count, and skipped count"
    - "An invalid --sector exits non-zero with a message listing valid sector names and runs no analysis"
    - "The root script contains no sector/history logic — it delegates to scanner.seasonality.load_sector_dataset"
  artifacts:
    - seasonality_by_week.py
    - tests/test_seasonality_cli.py
  key_links:
    - "seasonality_by_week.py imports and calls scanner.seasonality.load_sector_dataset"
    - "argparse defines --sector (required), --universe, --years, and declares --output/--bootstrap-iters/--seed for Phase 6/7"
---

<objective>
Create `seasonality_by_week.py` at the repo root: a thin CLI wrapper (mirroring `scan.py`'s thin-dispatcher
convention, D-04) that parses `--sector`/`--universe`/`--years`, delegates to
`scanner.seasonality.load_sector_dataset`, and prints a Phase-5 data-readiness summary (resolved sector,
matched/admitted/skipped counts). Invalid `--sector` exits non-zero listing valid names.

Purpose: Delivers the user-facing entry point for SEAS-01 (run the tool, get the sector's validated tickers)
and SEAS-02 (clear error exit on unknown sector). No statistics/output-formatting — those are Phases 6/7.
Output: `seasonality_by_week.py` and `tests/test_seasonality_cli.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sector-resolution-data-input/05-CONTEXT.md
@.planning/phases/05-sector-resolution-data-input/05-PATTERNS.md
@scanner/seasonality.py
</context>

<artifacts_this_phase_produces>
This plan produces:
- `seasonality_by_week.py` — new repo-root CLI entry point
- `build_parser() -> argparse.ArgumentParser` (flags: `--sector` required, `--universe` default sp500, `--years`, plus declared-for-later `--output`, `--bootstrap-iters`, `--seed`)
- `main(argv: list[str] | None = None) -> int`
- `tests/test_seasonality_cli.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create seasonality_by_week.py thin CLI</name>
  <files>seasonality_by_week.py</files>
  <read_first>
    - scan.py:249-266 (cmd_refresh — the thin-delegate-and-print pattern to mirror; logic stays in scanner/*)
    - scanner/seasonality.py (load_sector_dataset + valid_sectors, from Plan 02)
    - .planning/PROJECT.md §Current Milestone (the target CLI flag list: --sector, --years, --universe, --output, --bootstrap-iters, --seed)
  </read_first>
  <behavior>
    - `main(["--sector","Technology","--universe","sp500"])` calls load_sector_dataset once and prints a summary with the resolved sector name and the matched/admitted/skipped counts, returns 0
    - `main(["--sector","Widgets","--universe","sp500"])` prints the ValueError message (listing valid sectors) to stderr and returns a non-zero code, without calling load_sector_dataset's downstream work
    - --universe defaults to sp500 when omitted; --years is parsed as an optional int and forwarded
    - --output/--bootstrap-iters/--seed are declared in the parser (for Phase 6/7) but not consumed by Phase 5 logic
  </behavior>
  <action>
    Create `seasonality_by_week.py` at the repo root. Follow `scan.py`'s shape: a `build_parser()` returning an
    `argparse.ArgumentParser`, and a `main(argv=None)` that parses args, calls into `scanner.seasonality`, prints,
    and returns an int exit code. Keep ALL logic in `scanner.seasonality` — this file must contain no sector
    matching, no history checks, no yfinance import (D-04, thin-CLI convention).

    In `build_parser()`: add `--sector` (required=True), `--universe` (default="sp500"),
    `--years` (type=int, default=None). Also declare `--output` (default=None), `--bootstrap-iters` (type=int,
    default=None), and `--seed` (type=int, default=None) so the CLI surface anticipates Phase 6/7 (per PATTERNS.md
    Claude's-discretion note); add a help string on each noting they are consumed in later phases. Do not wire these
    three into any behavior here.

    In `main(argv=None)`: configure `logging.basicConfig(level=logging.INFO)` (so validate_history's skip summary is
    visible). Parse args. Wrap the pipeline call in try/except ValueError: call
    `ds = seasonality.load_sector_dataset(args.sector, args.universe, years=args.years)`. On `ValueError as exc`,
    `print(str(exc), file=sys.stderr)` and `return 2` (SEAS-02 — clear error exit, no analysis). On success, print a
    survivorship-neutral Phase-5 summary: the resolved `ds.sector`, `ds.universe`, `len(ds.frames)` admitted tickers,
    and `len(ds.skipped)` skipped, plus the first several skipped `(ticker, reason)` pairs. Return 0.

    Add the standard `if __name__ == "__main__": sys.exit(main())` guard. Import `sys`, `logging`, `argparse`, and
    `from scanner import seasonality`.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('seasonality_by_week.py').read()); print('ok')" && pytest -q tests/test_seasonality_cli.py</automated>
  </verify>
  <acceptance_criteria>
    - `python seasonality_by_week.py --help` exits 0 and shows --sector, --universe, --years, --output, --bootstrap-iters, --seed
    - `grep -c 'import yfinance' seasonality_by_week.py` returns 0
    - `grep -v '^#' seasonality_by_week.py | grep -c 'load_sector_dataset'` >= 1
    - The file defines no sector-matching or history-validation logic (delegates only)
  </acceptance_criteria>
  <done>A thin CLI parses the flags, delegates to load_sector_dataset, prints the readiness summary, and exits 2 with a valid-names listing on unknown --sector.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_seasonality_cli.py</name>
  <files>tests/test_seasonality_cli.py</files>
  <read_first>
    - tests/test_earnings_store.py:70-119 (monkeypatch test style)
    - seasonality_by_week.py (the CLI from Task 1)
  </read_first>
  <behavior>
    - Happy-path test: monkeypatch scanner.seasonality.load_sector_dataset to return a fake SectorDataset with 2 frames and 1 skipped; call main(["--sector","Technology","--universe","sp500"]); assert return 0 and captured stdout contains "Technology", the admitted count, and the skipped count
    - Invalid-sector test: call main(["--sector","Widgets","--universe","sp500"]) with load_sector_dataset left REAL (resolve_sector raises ValueError early); assert return code is non-zero and captured stderr lists valid sector names — and monkeypatch scanner.data_store.get_history to raise AssertionError to prove no analysis runs
    - Default-universe test: monkeypatch load_sector_dataset to record its args; call main(["--sector","Technology"]); assert it was called with universe "sp500"
  </behavior>
  <action>
    Create `tests/test_seasonality_cli.py`. Import the CLI module (e.g. `import seasonality_by_week as cli`). For the
    happy path, `monkeypatch.setattr("scanner.seasonality.load_sector_dataset", fake)` returning a real
    `SectorDataset` built via `scanner.seasonality.SectorDataset(sector="Technology", universe="sp500",
    frames={"AAPL": <tiny frame>, "MSFT": <tiny frame>}, skipped=[("XYZ","unresolved-sector")])`, use pytest's
    `capsys` to capture stdout, and assert the printed summary contains "Technology", "2", and "1". For the
    invalid-sector case, do NOT patch load_sector_dataset (let resolve_sector raise) but DO
    `monkeypatch.setattr("scanner.data_store.get_history", _raise)` so the test proves no history work happens; assert
    the return value is non-zero (== 2) and `capsys.readouterr().err` contains at least three valid sector names. For
    the default-universe test, capture the args passed to a fake load_sector_dataset and assert `universe == "sp500"`.
  </action>
  <verify>
    <automated>pytest -q tests/test_seasonality_cli.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/test_seasonality_cli.py` passes with at least 3 tests
    - Invalid-sector test asserts non-zero exit AND that get_history is never reached
    - Default-universe test proves sp500 is the default
    - `pytest -q` full suite stays green offline
  </acceptance_criteria>
  <done>The CLI's success summary, invalid-sector exit (SEAS-02), and default universe are covered by green offline tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → main() | user supplies --sector, --universe, --years, --output, --bootstrap-iters, --seed |
| ValueError message → stderr | the SEAS-02 error enumerates valid sector names to the operator |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Information Disclosure | invalid-sector error message | low | accept | The error deliberately lists the 11 valid GICS sector names — this IS the required SEAS-02 behavior and the data is non-sensitive (public sector taxonomy). No mitigation needed. |
| T-05-08 | Tampering | --output path handling | medium | accept | `--output` is declared in the parser for Phase 7 but performs NO file write in Phase 5, so no path-traversal surface is exercised yet. Phase 7 MUST validate/normalize the output path before writing — recorded here as a carry-forward for the Phase 7 threat model. |
| T-05-SC | Tampering | package installs | low | accept | No new package-manager installs in this phase — argparse and logging are stdlib; scanner deps already vendored. No legitimacy gate required. |
</threat_model>

<verification>
- `python seasonality_by_week.py --help` exits 0 and lists all six flags
- `pytest -q tests/test_seasonality_cli.py` green
- `pytest -q` full suite green offline
- Root script contains no yfinance import and no sector/history logic (delegation only)
</verification>

<success_criteria>
- SEAS-01: `seasonality_by_week.py --sector Technology --universe sp500` produces the sector's validated ticker set summary via the pipeline
- SEAS-02: unknown --sector exits non-zero listing valid names, no analysis run
- Root CLI is thin — all logic lives in scanner.seasonality (D-04)
</success_criteria>

<output>
Create `.planning/phases/05-sector-resolution-data-input/05-03-SUMMARY.md` when done.
</output>
